# EuropeUAConnectBot

Telegram forum platform for local Ukrainian communities across Europe.

User flow: /start → country → postal code → local place → Thread 1–10.

Production requires BOT_TOKEN. PostgreSQL via DATABASE_URL is recommended. The European postal catalogue is downloaded and built automatically on first start.
